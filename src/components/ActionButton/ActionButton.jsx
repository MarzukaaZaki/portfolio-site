import { Button } from '@mui/material';
import React from 'react';

const ActionButton = () => {
    return (
        <div>
            <Button></Button>
        </div>
    );
};

export default ActionButton;